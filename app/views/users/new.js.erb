if($("#body").html().length <= 1) {
  $("#body").html("<%= escape_javascript(render :partial => 'users/index') %>");
}
$("#actions").html("<%= escape_javascript(render :partial => 'users/form_actions' ) %>");
viewActionForm("", "", "", "new");
onReset();
