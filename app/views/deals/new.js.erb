if(($("#body").html().length <= 1) || (dataPage != null)) {
  $("#body").html("<%= escape_javascript(render :partial => 'deals/index') %>");
}
$("#deal_errors").html("<%= escape_javascript(render :partial => 'deals/form_errors') %>");
$("#deal_actions").html("<%= escape_javascript(render :partial => 'deals/form_actions' ) %>");
enabledElements();
canSelectDeal = false;
$("#deals_list").resetSelection();
loadDealElements();
