<% content_for :head do %>
  <title>Abstract</title>
<% end %>

<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path) %>
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>
  </li>
<% end %>

<p>
  <a href="#" onclick="$.ajax({url: 'entities'});">Entity</a>
  <a href="#" onclick="$.ajax({url: 'resources'});">Resource</a>
  <a href="#" onclick="$.ajax({url: 'deals'});">Deal</a>
  <a href="#" onclick="$.ajax({url: 'facts'});">Fact</a>
  <a href="#" onclick="$.ajax({url: 'charts'});">Chart</a>
  <a href="#" onclick="$.ajax({url: 'quotes'});">Quote</a>
  <a href="#" onclick="$.ajax({url: 'balances'});">Balance</a>
  <a href="#" onclick="$.ajax({url: 'general_ledgers'});">GeneralLedger</a>
  <a href="#" onclick="$.ajax({url: 'transcripts'});">Transcript</a>
</p>
<p>
  <a href="#" onclick="$.ajax({url: 'users'});">User</a>
  <a href="#" onclick="$.ajax({url: 'roles'});">Role</a>
</p>

<div id="body">
</div>
