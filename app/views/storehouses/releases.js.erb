document.title = "<%=t 'title.abstract'%> :: <%=t 'title.storehouse'%> :: <%=t 'title.release'%>";
$("#body").html("<%= escape_javascript(render :partial => 'storehouses/index') %>");
$("#storehouseDiv").html("<%= escape_javascript(render :partial => 'storehouses/releases') %>");
if(location.hash != "#storehouses/releases") {
  cancelRequest = true;
  location.hash = "#storehouses/releases";
}
if("<%= can?(:manage, Storehouse) %>" == "true") {
  $("#release_new").removeAttr("disabled");
  $("#release_new_1").removeAttr("disabled");
} else {
  $("#release_new").attr("disabled", "disabled");
  $("#release_new_1").attr("disabled", "disabled");
}

function filterGrid(state)
{
    var link = '/storehouses/releases/list';
    if(state != -1) {
        link += '?state=' + state
    }
    $("#view_release").attr("disabled", "disabled");
    $("#view_release_1").attr("disabled", "disabled");
    $('#releases_list').setGridParam({url: link});
    $('#releases_list').trigger('reloadGrid');
}
