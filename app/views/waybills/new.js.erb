$("#body").html("<%= escape_javascript(render :partial => 'waybills/form_actions') %>");
document.title = "Abstract::Waybills";
if(dataPage != null) {
  loadWaybillElements();
  dataPage = null;
} else {
  uin = 0;
  $("#waybills_list").addRowData(uin, { resource: ""
                                      , amount: ""
                                      , unit: "" });
  uin++;
}
