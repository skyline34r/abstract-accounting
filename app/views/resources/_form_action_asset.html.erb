<%= form_for(@resource, :url => { :action => "create_asset" }, :remote => true) do |f| %>
<%= f.submit "save", :id => "save_resource", :disabled => true,
                     :onclick => "sendTagAsset();" %>
<%= f.hidden_field :tag, :id => "hide_asset_tag", :value => @resource.tag %>
<% end %>
