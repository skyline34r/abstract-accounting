if(!crossPage.length) {
  $("#body").html("<%= escape_javascript(render :partial => 'resources/index') %>");
  document.title = "Abstract::Resource";
} else {
  $("#" + crossPage + "_page_selection").html("<%= escape_javascript(render :partial =>
                                                     'resources/index') %>");
}
